package com.example.itmproject.entities;

import javax.persistence.EnumType;
import javax.persistence.Enumerated;

/*public enum Type {
    operator,
    tech;

   public static Type toEnum(String type){
       if(type.equals(Type.operator.name())){
           return Type.operator;
       } else if (type.equals(Type.tech.name())) {
           return Type.tech;
       }
       return null;
   }
}

 */
